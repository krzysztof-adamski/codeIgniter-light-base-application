codeIgniter-light-base-application
==================================

Light CodeIgniter base application with twitter bootstrap and only mysql drive install .htaccess files already added and config setting just how I like it.
